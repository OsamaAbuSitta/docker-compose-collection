version: '3.8'
services:
  seq:
    image: datalust/seq:latest
    container_name: seq
    restart: unless-stopped
    environment:
      SEQ_FIRSTRUN_ADMINPASSWORD: "5NkX5baxKZvG3p8z"
      ACCEPT_EULA: "Y"
    ports:
      - "5341:80"
    volumes:
      - seq_data:/data

volumes:
  seq_data:
